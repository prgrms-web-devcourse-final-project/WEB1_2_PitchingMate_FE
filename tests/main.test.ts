import { describe, it, expect } from 'vitest'

describe('group', () => {
  it('should', () => {
    expect(1 + 1).toBe(2)
  })
})

describe('group', () => {
  it('should', () => {
    expect(1 + 1).toBe(2)
  })
})
